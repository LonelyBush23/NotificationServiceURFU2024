Для корректной работы EmailService и TelegramServise необходимо добавить .env файл в корень проекта

Пример `/.env`:

```
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
TELEGRAM_TOKEN=
```
